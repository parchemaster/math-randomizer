# Используем базовый образ с поддержкой PHP и веб-сервером
FROM php:8.2.4-cli

# Копируем исходные файлы приложения в контейнер
COPY . /var/www/html

# Устанавливаем зависимости и расширения PHP, если необходимо
# RUN apt-get update \
#     && apt-get install -y <название-пакетов>

# Открываем порт, если требуется
# EXPOSE <порт>

# Команда, которая будет выполнена при запуске контейнера
CMD ["apache2-foreground"]